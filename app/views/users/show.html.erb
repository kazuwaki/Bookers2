<div>
  <h3><%= @user.name %></h3>
 <p><%= image_tag @user.get_profile_image(100,100) %></p>
</div>

<% @books.each do |book| %>
<div>
  
  <p><%= image_tag book.user.get_profile_image(100,100) %></p>
  <p><%= book.title %></p>
  <p><%= book.body %></p>
</div>
<% end %>